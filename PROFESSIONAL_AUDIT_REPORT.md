# تقرير الفحص الاحترافي — تطبيق المحاسب الصوتي

**المشروع:** Smart Accountant / المحاسب الصوتي  
**الإصدار المفحوص:** v1.0.2  
**المستودع:** `alnwmbshyr1-cell/smart-accountant`  
**تاريخ الفحص:** 22 أغسطس 2026  
**إعداد التقرير:** Manus AI

## 1. الملخص التنفيذي

أُجري فحص متعدد الطبقات للنسخة الحالية، شمل مراجعة الكود والإعدادات، فحص الاعتمادات والأصول، تحليل دورة حياة Vosk، اختبارات المحلل المحلي وقاعدة SQLite، اختبار واجهة Flutter، فحص حزمة APK المنشورة، ومراجعة Workflow الخاص بالبناء السحابي.

النتيجة العامة: **النسخة قابلة للبناء والتثبيت، والتحليل والاختبارات الآلية ناجحة**. تم التحقق من APK منشور فعلياً على GitHub بحجم 98,358,722 بايت، وتطابقت بصمته SHA-256 مع البصمة المسجلة في GitHub. كما تم التأكد من أن نموذج Vosk غير مضمّن داخل APK، وأن المكتبات الأصلية الخاصة بـ Vosk موجودة للحزم الثنائية المتعددة.

مع ذلك، لا ينبغي اعتبار الفحص الآلي بديلاً عن اختبار جهاز Android حقيقي. توجد نقاط تشغيلية يجب اختبارها ميدانياً، أهمها تنزيل نموذج Vosk في أول تشغيل، صلاحية الميكروفون، استمرار الاستماع في الخلفية، توافق Android 13 أو أحدث، واختبار Gemini بمفتاح حقيقي. كما توجد ملاحظات جودة قابلة للتحسين، أبرزها انخفاض تغطية الاختبارات الحالية وظهور تحذير من حزمة `open_file` الخاصة بـ macOS.

## 2. مصفوفة النتائج

| المجال | النتيجة | التقييم | الملاحظات |
|---|---|---:|---|
| تحليل Dart/Flutter | `No issues found` | ناجح | لا توجد أخطاء أو تحذيرات تحليلية في الكود الحالي. |
| الاختبارات الآلية | 6 اختبارات ناجحة | ناجح | شملت الواجهة، قاموس اللهجة، المحلل، SQLite، وKeyset Pagination. |
| تغطية الأسطر | 34.69% | يحتاج تحسيناً | 383 من 1104 أسطر مغطاة وفق `lcov.info`. |
| APK Release | منشور بنجاح | ناجح | GitHub Actions اكتمل بنجاح في v1.0.2. |
| سلامة APK | SHA-256 متطابق | ناجح | البصمة: `f06c87b397edeb8470c0a7ae23b2e7d9ba01a1c17c1fd8890100cb4fcfcbc2e4`. |
| نموذج Vosk داخل APK | غير موجود | ناجح تصميمياً | النموذج يُحمّل عند أول تشغيل لتقليل حجم APK. |
| تشغيل Vosk بعد التنزيل | غير قابل للمحاكاة في Linux | يحتاج اختبار جهاز | يجب اختبار التنزيل وفك الضغط وإعادة الاستخدام على Android. |
| Gemini | مسار آمن من ناحية عدم تضمين المفتاح | يحتاج اختبار تكاملي | لم يُنفّذ طلب حي إلى Gemini لعدم توفر مفتاح اختبار. |
| الصلاحيات | الميكروفون والإنترنت والإشعار وخدمة المقدمة موجودة | يحتاج اختبار Android | يجب اختبار طلب الصلاحيات وسلوك الرفض. |
| Workflow | نجاح v1.0.2 | ناجح | يستخدم cache وإعادة المحاولة وJava 21. |
| التحسينات البرمجية | تحذير `open_file_macos` | منخفض | لا يمنع Android، لكنه يلوث مخرجات `pub get`. |

## 3. نطاق الفحص والبنية المفحوصة

تمت مراجعة الملفات الأساسية التالية:

| الملف أو المسار | ما تم فحصه |
|---|---|
| `lib/services/vosk_service.dart` | التهيئة، منع التهيئة المزدوجة، التحميل الشبكي، التسجيل، الإيقاف، و`dispose`. |
| `lib/ai_agent_service.dart` | Wake Word، التقاط الأمر لمدة 5 و10 ثوانٍ، Vosk، TTS، Gemini، والـ fallback المحلي. |
| `lib/ai_agent_parser.dart` | تصنيف المصروفات والمبيعات والمشتريات والديون والمخزون وتحليل الأرقام. |
| `lib/database_service.dart` | جدول العمليات، الإدخال، الإدخال الدفعي، Keyset Pagination، البحث، والإجماليات اليومية. |
| `lib/services/gemini_service.dart` | التخزين الآمن للمفتاح، بناء الطلب، تنظيف JSON، والتحقق من الرد. |
| `lib/main.dart` | زر المايك، حالات التهيئة والتسجيل، التنقل، الديون، المخزون، التقارير، والطباعة والتصدير. |
| `pubspec.yaml` | الاعتمادات، الإصدار، وعدم تضمين نموذج Vosk في assets. |
| `AndroidManifest.xml` | `RECORD_AUDIO` و`INTERNET` والإشعارات وخدمة الميكروفون في المقدمة. |
| `.github/workflows/build.yml` | Java 21، Flutter، cache، retries، التحقق من APK، وإنشاء Release. |

## 4. نتائج الاختبارات الوظيفية

أُعيد تشغيل الأمر التالي بنجاح:

```bash
flutter analyze
flutter test --concurrency=1 --coverage -r expanded
```

وكانت النتيجة:

```text
No issues found!
00:06 +6: All tests passed!
```

الاختبارات الناجحة تضمنت اختبار واجهة smoke، تطبيع ألفاظ اللهجة اليمنية، تحليل أمر مبيعات باللهجة المحلية، حفظ عملية في SQLite، اختبار Keyset Pagination، واختبار خدمة العزل القديمة الخاصة بـ Gemma. تم تحديث توقع التنقل للمبيعات إلى شاشة المبيعات والمشتريات، كما تم إصلاح أولوية عبارة «بعت بضاعة» حتى لا تُصنف كمخزون.

### تغطية الاختبارات

تم إنشاء `coverage/lcov.info`، وكانت النتيجة المحسوبة:

| المؤشر | القيمة |
|---|---:|
| الأسطر القابلة للتغطية | 1104 |
| الأسطر المنفذة في الاختبارات | 383 |
| التغطية | 34.69% |

هذه النسبة مقبولة كخط أساس، لكنها غير كافية لإصدار محاسبي حساس. يوصى برفعها إلى 70% على الأقل قبل الاعتماد الإنتاجي، مع تغطية خاصة لمسارات الفشل وليس فقط المسارات الناجحة.

## 5. فحص الأوامر الصوتية

المحلل المحلي يدعم الأنواع التالية، ويرجع `targetTab` مناسباً لكل نوع:

| مثال الأمر | النوع المتوقع | الشاشة المستهدفة |
|---|---|---|
| «سجل مصروف بنزين بعشرين ألف» | `مصروف` | الرئيسية |
| «بعت بضاعة بمئة ألف» | `مبيعات` | المبيعات والمشتريات |
| «اشتريت بضاعة بخمسين ألف» | `مشتريات` | المبيعات والمشتريات |
| «دين لي على خالد بمئة ألف» | `دين_لي` | الديون |
| «سجلت دين علي للمورد بعشرة آلاف» | `دين_علي` | الديون |
| «أضف عشرة كراتين للمخزن» | `مخزون` | المخزون |

تم اختبار أمر المبيعات باللهجة المحلية نصياً، وليس عبر ميكروفون Android فعلي. لذلك لا يمكن من الاختبارات الحالية إثبات دقة Vosk الصوتية مع نطق حقيقي أو ضوضاء أو اختلافات نبرة الصوت.

## 6. فحص دورة حياة Vosk

تمت معالجة سبب `LateInitializationError: Field '_vosk' has already been initialized` عبر فصل plugin عن الإنشاء المبكر، واستخدام إنشاء كسول عند الحاجة، بالإضافة إلى حارس التهيئة المشترك:

```dart
bool _isInitialized = false;
Future<void>? _initializationFuture;
```

هذا يمنع التهيئة المكررة عندما يصل الطلب من بدء التطبيق أو Wake Word أو زر المايك في الوقت نفسه. كما أن الخدمة تحتوي على إيقاف للمسجل، حذف الاشتراكات، تحرير recognizer/model، و`dispose` آمن.

الملاحظة التشغيلية المهمة هي أن نموذج Vosk غير موجود في APK. عند أول استخدام يجب توفر الإنترنت ومساحة كافية لفك النموذج؛ بعد اكتمال التنزيل يمكن العمل دون اتصال. يجب عرض تقدم التنزيل ورسالة فشل قابلة لإعادة المحاولة في واجهة الإنتاج، لأن الخدمة الحالية لا تثبت عبر الاختبارات وجود تجربة تنزيل مرئية على جهاز حقيقي.

## 7. فحص APK المنشور

تم تنزيل APK الإصدار v1.0.2 من GitHub والتحقق من سلامته:

| المؤشر | القيمة |
|---|---|
| الملف | `app-release.apk` |
| الحجم | 98,358,722 بايت، نحو 94MB |
| SHA-256 | `f06c87b397edeb8470c0a7ae23b2e7d9ba01a1c17c1fd8890100cb4fcfcbc2e4` |
| Release | `v1.0.2` |
| رابط الإصدار | https://github.com/alnwmbshyr1-cell/smart-accountant/releases/tag/v1.0.2 |
| رابط التنزيل | https://github.com/alnwmbshyr1-cell/smart-accountant/releases/download/v1.0.2/app-release.apk |

فحص محتويات APK أظهر وجود مكتبات `libvosk.so` و`libapp.so` و`libflutter.so` للحزم `arm64-v8a` و`armeabi-v7a` و`x86_64`. كما لم يظهر أي مسار `vosk-model` أو `gemma` داخل APK، وهو متوافق مع استراتيجية التحميل عند أول تشغيل.

## 8. فحص البناء وGitHub Actions

Workflow الإصدار v1.0.2 اكتمل بنجاح، وكانت خطوات `Setup Flutter` و`Install dependencies` و`Build APK` و`Verify APK` و`Create Release and Upload APK` ناجحة. يستخدم workflow حالياً Java 21، وFlutter 3.27.1، وGradle cache، وإعادة محاولة لتنزيل الاعتمادات والبناء.

البناء المحلي داخل الساندبوكس ما زال غير موثوق بسبب محدودية المساحة؛ ظهرت سابقاً أخطاء `No space left on device` وملفات Kotlin DSL ناقصة. لذلك تم الاعتماد في النسخة المنشورة على GitHub Actions، ونجح البناء السحابي فعلياً.

## 9. فحص الأمان والخصوصية

لم يظهر في فحص النصوص مفتاح Gemini صريح أو قيمة `AIza...` داخل ملفات `lib` أو `android`. خدمة Gemini تقرأ المفتاح من `flutter_secure_storage` ولا تضعه في `pubspec.yaml` أو المستودع.

مع ذلك، تخزين مفتاح API داخل تطبيق جوال لا يجعله سرياً بشكل مطلق؛ يمكن لمستخدم متقدم استخراج المفتاح من جهازه أو من الذاكرة. الحل الإنتاجي الأفضل هو خادم وسيط يضع حدود الحصة ويتحقق من المستخدمين، أو استخدام مفتاح محدود الصلاحيات والحصة مخصص للتطبيق مع مراقبة مستمرة.

الصلاحيات المعلنة تشمل تسجيل الصوت والإنترنت والإشعارات وخدمة الميكروفون في المقدمة وقفل الاستيقاظ. يجب مراجعة ضرورة `FOREGROUND_SERVICE_MICROPHONE` وفق إصدار Android المستهدف واختبار طلبها من الجهاز، خصوصاً عند تشغيل الاستماع في الخلفية.

## 10. فحص الأداء وقابلية التوسع

استخدام Keyset Pagination موجود في قاعدة البيانات، وهذا أفضل من `OFFSET` عند التصفح المتدرج للبيانات الكبيرة. كما يوجد إدخال دفعي مناسب لعمليات seed. لم يُنفذ خلال هذا الفحص اختبار تحميل حقيقي بمليون أو عشرة ملايين سجل، لذلك لا يجوز اعتبار أداء 10M مثبتاً من الاختبارات الحالية.

نقاط الأداء الإيجابية هي وجود مؤشرات SQLite، التصفح بالـ cursor، وعدم تضمين نموذج الصوت داخل APK. أما نقاط التحسين فهي تشغيل إدخالات seed خارج isolate أو عبر worker مخصص، إضافة قياسات زمنية تلقائية للبحث والإدخال، وعدم تحميل قائمة طويلة كاملة إلى الذاكرة في شاشة المخزون أو الديون.

## 11. الملاحظات والمخاطر المتبقية

| الأولوية | الملاحظة | الأثر | الإجراء المقترح |
|---|---|---|---|
| عالية | لم يُختبر الصوت على جهاز Android فعلي في هذا الفحص | قد تظهر اختلافات plugin أو صلاحيات أو صوت | اختبار على Android 10 و13 و14 و15 مع سماح ورفض الميكروفون. |
| عالية | تنزيل Vosk أول مرة يحتاج إنترنت ومساحة كبيرة | قد يفشل الإعداد الأول أو يترك ملفاً ناقصاً | إضافة progress، checksum، استئناف، وحذف الملف المؤقت عند الفشل. |
| عالية | لا توجد تغطية كافية لمسارات الفشل | أخطاء الإنتاج قد لا تكون ممثلة | رفع التغطية إلى 70% وإضافة اختبارات للانقطاع، JSON غير صالح، وقاعدة البيانات. |
| متوسطة | `open_file` يطبع تحذير macOS أثناء pub get | ضوضاء في CI ولا يمنع Android حالياً | تحديث الحزمة أو استبدالها بحزمة مدعومة أو تثبيت إصدار مصحح. |
| متوسطة | workflow لا يشغل analyze وtest قبل بناء APK | قد يُبنى APK رغم تراجع جودة الكود | إضافة خطوات analyze وtest إلى workflow قبل Gradle. |
| متوسطة | Gemini يعتمد على API key داخل العميل | المفتاح قابل للاستخراج نظرياً | نقل Gemini إلى backend للإصدار التجاري. |
| منخفضة | لا يوجد اختبار UI آلي لجلسة الميكروفون الحقيقية | تغطية تجربة المستخدم الصوتية محدودة | إضافة اختبار جهاز/Integration Test مع mock للصوت. |

## 12. التوصيات ذات الأولوية

أول توصية هي إضافة اختبارات Integration على جهاز Android حقيقي لمسار كامل: فتح التطبيق، منح الميكروفون، تنزيل النموذج، قول «يا محاسب»، قول أمر بيع أو دين، التأكد من الحفظ في SQLite، ثم التحقق من الرد الصوتي والتنقل.

ثاني توصية هي تعديل Workflow ليشغل `flutter analyze` و`flutter test --coverage` قبل `flutter build apk --release`، مع فشل البناء إذا انخفضت التغطية أو ظهرت أخطاء تحليلية. ثالث توصية هي تحسين تنزيل Vosk ليكون قابلاً للاستئناف ومتحققاً من الحجم أو checksum، مع إظهار نسبة التقدم للمستخدم.

رابع توصية هي إنشاء اختبارات منفصلة للأنواع الستة ولصيغ الأرقام المركبة مثل «مليونين وخمسمية»، مع حالات النفي، غياب المبلغ، اسم غير موجود، مبلغ عشري، وانقطاع Gemini. خامس توصية هي نقل Gemini إلى خادم وسيط قبل استخدام التطبيق على نطاق تجاري واسع.

## 13. الحكم النهائي

**الحكم:** النسخة الحالية **اجتازت الفحص الآلي والبناء السحابي، وهي صالحة كنسخة تجريبية قابلة للتثبيت**. لا توجد أخطاء Dart أو اختبارات آلية فاشلة، وAPK المنشور سليم ومتطابق البصمة.

لكنها ليست مصدّقاً عليها بعد كنسخة إنتاج محاسبية نهائية، لأن اختبار الجهاز الحقيقي، اختبار تنزيل نموذج Vosk، اختبار الضوضاء الصوتية، اختبار الاستماع في الخلفية، واختبار الأحمال الكبيرة لم تُنجز في بيئة Android فعلية ضمن هذا الفحص. مستوى الجاهزية الحالي يُقدّر بأنه **جاهزية تجريبية قوية، مع حاجة إلى تحقق ميداني قبل الاعتماد التجاري**.

## المرفقات والأدلة

- تقرير الاختبارات الخام: `/tmp/smart_accountant_test_report.txt`
- تقرير فحص APK: `/tmp/smart_accountant_release_audit.txt`
- تقرير فحص الأمان والاعتمادات: `/tmp/smart_accountant_security_audit.txt`
- تقرير دورة الحياة: `/tmp/smart_accountant_runtime_audit.txt`
- تغطية الاختبارات: `/home/ubuntu/smart_accountant_repo/coverage/lcov.info`
- APK المنشور: https://github.com/alnwmbshyr1-cell/smart-accountant/releases/download/v1.0.2/app-release.apk
